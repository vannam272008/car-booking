import React from 'react';

function MenuAdd() {
    return (
        <RequestLayout profile={profile}>
            {() => (
                <div>

                </div>
        </RequestLayout>
    );
}

export default MenuAdd;